package game.utility;

/**
 * Functional interface for logging game events and messages.
 */
public interface Logger {

    /**
     * Logs the specified message.
     *
     * @param text the message to log
     */
    void log(String text);
}
